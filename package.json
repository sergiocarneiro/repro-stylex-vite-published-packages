{
  "name": "repro-stylex-vite-published-packages",
  "description": "This is not a monorepo, use projects individually.",
  "scripts": {
    "clean": "rm -rf **/build **/dist **/node_modules *.tgz",
    "postinstall": "npm --prefix library install && npm --prefix app install"
  }
}
